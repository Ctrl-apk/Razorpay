fastapi>=0.104.0
uvicorn>=0.24.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
httpx>=0.25.0
python-dateutil>=2.8.2

# Optional LLM providers — only needed when AI_PROVIDER=openai or anthropic
# openai>=1.3.0
# anthropic>=0.7.0

# PostgreSQL driver — only needed when using postgres DATABASE_URL
# psycopg2-binary>=2.9.9

# Test dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
